#include "heap.h"
#include <stdio.h>

void bubble_up(int* heap, int no){

    //condicao de parada

    int pai = no/2;
}

void inserir(int *heap){

    int ultimo = (++heap[0]);
    //heap[ultimo] = chave;

}

int frente(int* heap){
    return heap[1];
}